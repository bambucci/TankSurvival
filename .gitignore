# Ignore everything in the repository
/*

# Except the Assets/Scripts folder
!/Scripts/
!/Scripts/**/*

# Ignore all .meta files
**/*.meta

# Optional: Include .gitignore itself (so it gets tracked)
!.gitignore